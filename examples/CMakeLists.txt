cmake_minimum_required(VERSION 3.5 FATAL_ERROR)

# Find the Ignition library
find_package(ignition-sensors0 REQUIRED)

# add_executable(EXAMPLE_EXECUTABLE SOURCE.cc)
# target_link_libraries(EXAMPLE_EXECUTABLE ${IGNITION-SENSORS_LIBRARIES})
